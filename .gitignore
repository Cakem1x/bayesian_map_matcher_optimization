# vim temp files
*.swp
# Compiled python modules.
*.pyc
# Setuptools distribution folder.
/dist/
# Python egg metadata, regenerated from source files by setuptools.
/*.egg-info
# ignore python pickle files (may get generated when running example code)
*.pkl
# ignore svg (may get generated when running example code)
*.svg
# ignore specific yaml files that get generated when running example code
*iteration.yaml
# ignore specific markdown files that get generated when running example code
sampled_params.md
